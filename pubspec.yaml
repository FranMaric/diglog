name: diglog
description: Flutter web app that helps you with subject "Digitalna logika"

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^4.3.3
  firebase_core: "^0.7.0"
  cloud_firestore: "^0.16.0+1"
  platform_detect: ^1.4.0
  url_launcher: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
